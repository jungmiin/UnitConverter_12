# UnitConverter_12 — REFACTOR·추적성·B-ERR-02 (P0 마감)

| 항목 | 내용 |
|------|------|
| 프로젝트 | UnitConverter_12 |
| 단계 | R — Refine (REFACTOR) + P0-VAL-NEG golden master 보강 |
| 보고서 생성일 | 2026-06-11 |
| 목적 | `/refactor-smell`·`/refactor-safe` R1–R4, `/traceability`, B-ERR-02 강화로 P0 범위 마감 |

관련 Transcript: [Prompting/06.Export-Transcript.md](../Prompting/06.Export-Transcript.md)

---

## 1. 요약

본 세션은 GREEN 완료 후 REFACTOR 단계를 수행하고 P0 추적성을 확보한 뒤, 마지막 P0 갭(`P0-VAL-NEG` / `B-ERR-02`)을 golden master로 고정했다.

`/refactor-smell`로 `UnitConverter.py` baseline 스멜 10건을 분석하고, `/refactor-safe` R1–R3(비율 상수 단일화, `cli.main()` 구현, `UnitConverter.py` thin wrapper)를 적용했다. 전후 pytest **20 passed** 유지.

`/traceability`로 Concept → Requirement → Test → Code C2C 표를 작성하고 P0/P1 갭을 명시했다. `B-ERR-02` 음수 검증 golden master를 `/red-skeleton` + R4로 강화해 parser·`run()`·`B-CLI-02` 4종 오류 메시지를 고정했다. 최종 pytest **22 passed / 0 failed**.

---

## 2. 핵심 결정·산출물

| 산출물 | 내용 |
|--------|------|
| REFACTOR R1 | `METER_TO_*`, `FEET_TO_METER`, `YARD_TO_METER` → `unit_registry.py` 단일 정의; 테스트 import |
| REFACTOR R2 | `cli.main()` — `input()` → `run()` → `print()`; 예외 시 `str(exc)` 출력 |
| REFACTOR R3 | `UnitConverter.py` → `unit_converter.cli.main` 위임 (5줄) |
| REFACTOR R4 | `NEGATIVE_VALUE_MESSAGE` 상수 (`input_parser.py`); B-ERR-02 parser·run·B-CLI-02 고정 |
| C2C 추적표 | P0 18행 + P1 4행; SC-1~3 매핑; 갭 3종(음수→해소, main 직접 테스트, P1 미구현) |
| 테스트 | 20 → 22건 (`test_b_err_02_negative_value_parser`, `_run` 분리; `B-CLI-02`에 `meter:-1` 추가) |

### SC 매핑 (세션 후)

| 성공 기준 | 대표 테스트 | 상태 |
|-----------|-------------|------|
| SC-1 수정 지점 한 흐름 | `D-OCP-01`, `D-REG-02` | GREEN |
| SC-2 입력·출력 비율 일관 | `D-CONV-02/03/04` | GREEN |
| SC-3 수동 실행 없이 회귀 | `B-CLI-01~04`, `B-FMT-01`, `B-CLI-02`(4종 오류) | GREEN |

### Golden Master 오류 메시지 (고정)

| 입력 | 메시지 |
|------|--------|
| `meter2.5` | `Invalid format. Use unit:value (ex: meter:2.5)` |
| `meter:abc` | `Invalid number: abc` |
| `meter:-1` | `Negative value not allowed` |
| `cubit:1` | `Unknown unit: cubit` |

---

## 3. 다음 단계

1. **`/export`** — 본 세션 Transcript·Report (완료: 06)
2. **P1 시작 시** `/red-test-plan` — ConfigLoader, 동적 등록, JSON/CSV/TABLE 출력 범위
3. **선택** — `cli.main()` smoke test 1건 추가

---

*본 문서는 Report/06.REPORT.md — UnitConverter_12 REFACTOR·추적성·P0 마감 세션 보고서입니다.*
